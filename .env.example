# Server Configuration
PORT=10000
NODE_ENV=production

# Database & Storage
# On platforms like Render or Railway, you should mount a persistent volume 
# to the /app/uploads and /app/blocks.db to avoid data loss on restarts.
